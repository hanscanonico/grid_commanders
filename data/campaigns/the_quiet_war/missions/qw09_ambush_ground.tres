[gd_resource type="Resource" script_class="MissionDefinition" load_steps=4 format=3]

[ext_resource type="Script" path="res://core/campaign/mission_definition.gd" id="1"]
[ext_resource type="Script" path="res://core/campaign/objectives/survive_until_day_objective.gd" id="2"]

[sub_resource type="Resource" id="Objective_survive_the_ambush"]
script = ExtResource("2")
text = "Survive until day 4."
day = 4

[resource]
script = ExtResource("1")
id = &"qw09_ambush_ground"
title = "Ambush Ground"
location = "An unmarked wood, Verdant border"
map_path = "res://maps/campaign/the_quiet_war/qw09_ambush_ground.txt"
player_team = 1
ai_teams = Array[int]([2])
seats = Array[int]([])
sides = {}
commanders = {
1: &"ines_calder",
2: &"cass_orlov"
}
fog_enabled = true
difficulty = &"normal"
objectives = Array[Resource]([SubResource("Objective_survive_the_ambush")])
failures = Array[Resource]([])
bonus_objectives = Array[Resource]([])
par_day = 4
briefing = Array[String]([
"Orlov's raiders picked this wood because the map calls it empty ground. It has not been empty since Ines Calder's column moved through it.",
"A cheaper, smaller force reaches the tree line first — the one thing this ambush never accounted for."
])
victory = Array[String]([
"The ambush springs on ground it cannot hold. Calder's column was never where the map said it would be.",
"Orlov's raiders scatter into terrain that no longer favours them either."
])
defeat = "The ambush caught the column exactly where the map promised it would be."
