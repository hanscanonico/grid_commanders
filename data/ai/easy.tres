[gd_resource type="Resource" script_class="AIProfile" load_steps=2 format=3]

[ext_resource type="Script" path="res://ai/ai_profile.gd" id="1"]

; Easy. Bad judgement, never a handicap: the same income, vision and dice as
; every other tier — it simply reads the board worse.
;
; threat_aversion is the timidity dial, turned the opposite way from Difficult's.
; The DF4 ladder found that over-weighting danger is what actually makes this
; planner weak (0.5 alone loses 88% of its games — docs/difficulty_check.md §6;
; the standing verdict is at the top of that file and §4c is the current
; ladder), and "flinches from danger" is
; exactly the character Easy wants: it hangs back, refuses trades it should take,
; and lets the player dictate the fight. advance_threat_tiles is the same
; flinch on the advance path, and Easy gets the larger of the two shipped values
; because hanging back is its whole character. The remaining weights say the rest —
; poor finishing, fears every counter, runs home early, shrugs at marginal plays,
; stops at two capture units even on a large property race, and never fields an
; md tank. The tail appends Normal's non-transport air and sea combat units in
; their existing order, so an owned airport or port stays useful. It adds no land
; unit — the land prefix above is Easy's alone — and no transport, which the
; planner cannot use.
;
; Rockets join at the very end, below everything, on every tier: new entries go
; to the tail because a doctrine's build_bias counts in places on this list, so
; sorting one into the middle silently weakens every bias aimed below it. On Easy
; the tail is also where they belong — under-valuing the expensive indirect is the
; same judgement that leaves the md tank off entirely. The recon stays off every
; list on purpose; see build_priority's own comment.
;
; supply_weight is the same bad judgement rather than a missing capability: well
; under Normal's, so an APC leaves a half-empty rack until it is nearly bare.
; Keeping the front stocked is a patient habit, and patience is what Easy lacks.
[resource]
script = ExtResource("1")
kill_bonus = 1.0
counter_weight = 1.0
capture_score = 900.0
hq_capture_multiplier = 3.0
capture_progress_bonus = 45.0
step_cost_penalty = 4.0
retreat_hp = 60
min_useful_score = 80.0
advance_score = 1.0
build_priority = Array[StringName]([&"tank", &"artillery", &"mech", &"bomber", &"battleship", &"fighter", &"sub", &"cruiser", &"b_copter", &"rockets"])
capture_unit_target = 2
air_answer_ids = Array[StringName]([&"anti_air", &"missiles", &"fighter", &"cruiser"])
air_answer_target = 2
supply_unit_target = 1
duplicate_priority_cost = 3
save_up_turns = 2
dive_score = 200.0
refuel_margin_turns = 1
doctrine_weight = 1.0
threat_aversion = 0.3
advance_threat_tiles = 3.0
focus_fire_bonus = 0.0
build_reactivity = 0.0
defend_weight = 0.0
withdraw_weight = 0.0
cohesion_tiles = 0.5
cohesion_radius = 4
capture_units_per_property = 0.0
capture_claim_depth = 0
production_capture_multiplier = 1.0
capture_goal_value_tiles = 0.0
spend_ceiling_turns = 0.0
bank_scope = 0
bank_rank_margin = 0
supply_weight = 0.1
cover_tiles = 0.0
condition_weight = 0.0
join_weight = 0.0
goal_engageability = 0.0
capture_threat_aversion = 0.0
standoff_band_tolerance = 0
