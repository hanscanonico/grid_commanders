[gd_resource type="Resource" script_class="Difficulty" load_steps=3 format=3]

[ext_resource type="Script" path="res://core/difficulty.gd" id="1"]
[ext_resource type="Resource" path="res://data/ai/default.tres" id="2"]

; Normal's weights live in data/ai/default.tres, which is also AIProfile's
; missing-file fallback — that header says why the two are one file.

[resource]
script = ExtResource("1")
id = &"normal"
display_name = "Normal"
ai_profile = ExtResource("2")
